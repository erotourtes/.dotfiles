#!/usr/bin/env fish

rofi -i -show drun
  # -modi "drun,Clipboard:~/.config/sway/clipboard-manager.bash" \
  # -modi "drun,Clipboard:~/.config/sway/clipboard-manager.bash,window,run,filebrowser" \
  # -kb-custom-1 "Control-Delete" \
  # -kb-custom-2 "Alt-Delete"
